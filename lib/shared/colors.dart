import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

//Color greenoil = const Colors;
Color cyan = Color.fromRGBO(195, 224, 229, 1);
Color bluedark = Color.fromRGBO(29, 68, 114, 1);
Color bluemod = Color.fromRGBO(88, 133, 175, 1);
Color bluedarkk = Color.fromRGBO(40, 90, 130, 1);
/*
Color lightcyan
Color darkblue
Color modblue*/
final DateTime now = DateTime.now();
final DateFormat formatter = DateFormat('yyyy-MM-dd');
final String formatted = DateFormat('yyyy-MM-dd').format(DateTime.now());
